{
  "name": "trsalliance-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",                      // 🔁 Required for Vercel (used by "npm run build")
    "preview": "vite preview",
    "seo": "node cli/index.js seo --agent=deepseek --target=meta"  // optional: CLI scan alias
  },
  "dependencies": {
    "vite": "^5.0.0",                          // 🔁 Adjust based on actual version
    "some-framework": "^1.0.0"                 // e.g., react, vue, svelte — add your actual framework
  },
  "devDependencies": {
    "@vitejs/plugin-xxx": "^x.x.x"             // 🔁 Include Vite plugins you're using
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "trsalliance-seo",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seo": "node cli/index.js seo --agent=deepseek --target=meta",
    "audit": "npm run seo && echo 'SEO Audit Complete ✅'",
    "deploy": "npm run build && npx netlify deploy --prod --dir=dist"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@netlify/cli": "^17.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
